apiVersion: v1
kind: Service
metadata:
  name: multiagent-app
  namespace: {{ .Values.namespace }}
  labels:
    app: multiagent-app
spec:
  type: ClusterIP
  selector:
    app: multiagent-app
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
